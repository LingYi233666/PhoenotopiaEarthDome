local GaleSpDamageBase = require("components/gale_spdamage_base")

local GaleSpDamagePsychic = Class(GaleSpDamageBase, function(self, inst)
    GaleSpDamageBase._ctor(self, inst)
end)

return GaleSpDamagePsychic
